version: "3.9"

services:
  inkling-api:
    build:
      context: ../..
      dockerfile: inkling/integration/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    command: ["uvicorn", "inkling.integration.api:app", "--host", "0.0.0.0", "--port", "8000"]
